import { Spinner } from '@material-tailwind/react';
import React from 'react';

const Loader = () => {
    return (
        <div>
            <Spinner />
        </div>
    );
};

export default Loader;